{% set name = "scriptengine" %}
{% set version = "0.13.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/scriptengine-{{ version }}.tar.gz
  sha256: e4411ee7fd6693745c7732330b843411661cd60b621ee930a0d61b32f6cf5182

build:
  entry_points:
    - se = scriptengine.cli.se:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - deepdiff >=5.7.0,<6.2.1
    - deepmerge
    - jinja2
    - python >=3.6
    - python-dateutil
    - pyyaml

test:
  imports:
    - scriptengine
  commands:
    - pip check
    - se --help
  requires:
    - pip

about:
  home: https://github.com/uwefladrich/scriptengine
  summary: A lightweight and extensible framework for executing scripts written in YAML
  license: GPL-3.0-or-later
  license_file: LICENSE

extra:
  recipe-maintainers:
    - uwefladrich
    - zklaus
